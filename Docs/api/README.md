# API Contracts

**Define contracts here before implementing endpoints.** Use the `api-contract-first` skill.

One file per resource or endpoint group, e.g. `assistants.md`, `auth.md`.

OpenAPI/Swagger may live in `apps/api/` when the API exists.
