# Testing

Standards: `.cursor/rules/testing-standards.mdc`

| Layer | Scope |
|-------|--------|
| Unit | Services, validators, utilities |
| Integration | API, DB, Redis, AI (mocked) |
| E2E | Auth, CRUD, billing, knowledge, AI flows |

Add test plans per feature in `features/` when shipping.
