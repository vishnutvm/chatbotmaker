# Security

Standards: `.cursor/rules/security-rules.mdc`

Every feature: auth, authorization, tenant isolation, input/output validation, rate limiting, secrets, audit logging.

Run `security-engineer` subagent before marking features complete.
