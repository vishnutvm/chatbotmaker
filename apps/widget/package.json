{
  "name": "@genie/widget",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"No lint configured for @genie/widget\"",
    "test": "pnpm build && node --test scripts/widget.test.mjs",
    "smoke": "pnpm build && node --test scripts/widget.test.mjs"
  },
  "devDependencies": {
    "@genie/config": "workspace:*",
    "linkedom": "0.18.11",
    "tsup": "^8.4.0",
    "typescript": "^5.7.2"
  }
}
